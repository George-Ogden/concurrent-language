fib = (n: int) -> int {
    if (n <= 1){
        if (n < 0) {
            0
        } else {
            1
        }
    } else {
        fib(n-1) + fib(n-2)
    }
};

main = (n: int) -> int {
    fib(n)
};
