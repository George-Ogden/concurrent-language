main = (a: int, b: int, c: int, d: int) -> int {
    a0 = a / b;
    a1 = a / c;
    a2 = a / d;
    a3 = b / c;
    a4 = b / d;
    a5 = c / d;
    b0 = a / b / c;
    b1 = a / b / d;
    b2 = b / c / d;
    (a0 + a1 + a2 + a3 + a4 + a5 + b0 + b1 + b2) / 2
}
